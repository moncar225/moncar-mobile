/// MON CAR — lectures/écritures QR (billets, colis) pour les apps mobiles.
///
/// Squelette mince autour de `mobile_scanner` : un scanner plein écran
/// réutilisable. La caméra n'est jamais activée hors écran de scan explicite.
library;

export 'src/moncar_qr_scanner.dart';
