import 'package:flutter_test/flutter_test.dart';

void main() {
  test('squelette : la bibliothèque se charge sans erreur', () {
    // Le schéma Drift sera introduit avec le contrat OpenAPI du backend.
    // Ce test garantit que le package reste importable en l'état.
    expect(true, isTrue);
  });
}
